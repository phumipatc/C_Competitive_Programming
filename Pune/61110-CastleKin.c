#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<ctype.h>
#include<string.h>
//int b[100000];
int main(){
    int n,b,i,ans;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&b);
            ans = (2*(n*b)+(2*b)+n);

    }
    printf("%d",ans);
    return 0;
}
